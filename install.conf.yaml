- clean: ['~/']

- link:
    ~/.bash_profile:
        relink: true
        force: true
        backup: true
        path: bash_profile.sh
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Updating submodules]
